= form_with url: subscriptions_path, local: true, id: "plan_switch_form", method: :patch do
  .input-group
    = select_tag :plan_id, options_from_collection_for_select(@plans, "id", "name", current_user.plan_id), { class: "custom-select" }
    .input-group-append
      %button.btn.btn-outline-success Switch
